为什么会出现粘包？

主要原因：tcp数据传递模式是stream流模式，在保持长连接的时候可以进行多次的收和发

“粘包”可发生在发送端也可发生在接收端
    1. 由Nagle算法造成的发送端的粘包：Nagle算法是一种改善网络传输效率的算法。
简单来说，当我们提交一段数据时，TCP不会立刻发送，而是看等待期间是否还有其他数据；若有，
则会把两段数据一起发送出去。

    2. 接收端接收不及时也会造成接收端的粘包：TCP把收到的数据存在缓冲区并通知应用层取数据。
当应用层由于某些原因不能及时的把TCP数据取出来，就会造成缓冲区中存放了多段数据。